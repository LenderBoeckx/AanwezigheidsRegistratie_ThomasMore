@using Microsoft.AspNetCore.Identity
@using Boekingssysteem.Areas.Identity.Data

@inject SignInManager<CustomUser> SignInManager
@inject UserManager<CustomUser> UserManager

<div class="col">
    <ul class="navbar-nav">
        @if (SignInManager.IsSignedIn(User))
        {

            <li class="nav-item mt-5 mr-5">
                <a id="manage" class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage" style="font-size: 20px">Hello @UserManager.GetUserName(User)!</a>
            </li>
            <li class="nav-item mt-2 mr-5">
                <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                    <div>
                        <button id="logout" type="submit" class="btn btn-primary btn-lg mb-2 mt-3">Logout</button>
                    </div>
                </form>
            </li>
        }
        else
        {
            @* <li class="nav-item">
        <div>
        <a class="btn btn-primary btn-lg mb-2 mt-5 mr-5" id="register" asp-area="Identity" asp-page="/Account/Register">Registreer admin</a>
        </div>
        </li>
        <li class="nav-item">
        <div>
        <a class="btn btn-primary btn-lg mb-2 mr-5" id="login" asp-area="Identity" asp-page="/Account/Login">Inloggen admin</a>
        </div>
        </li>*@
        }
    </ul>
</div>